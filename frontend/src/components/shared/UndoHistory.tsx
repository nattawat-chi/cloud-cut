import { HistoryPanel } from '@/components/overlays/HistoryPanel';

/**
 * Shared undo history widget — thin re-export of HistoryPanel for use
 * as an inline panel rather than a floating overlay.
 */
export { HistoryPanel as UndoHistory };
